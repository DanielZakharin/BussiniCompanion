import { Text } from "react-native"
import Screenroot from "../../common/elements/screenroot"

export default () => {
    return <Screenroot>
        <Text>
            Hello this will be the pattern page
        </Text>
    </Screenroot>
}